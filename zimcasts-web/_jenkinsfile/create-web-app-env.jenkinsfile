pipeline {
    agent any
    stages {
        stage('Create Web App Resources') {
            steps {
                sh 'docker compose up -f  ../compose-web-env.yml -d --build'
            }
        }
        stage('Clean up') {
            steps {
                deleteDir()
            }
        }
    }
}